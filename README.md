# NodeJS-Database-Extractor

Extracts data from a PostgreSQL table and generates a file in .parquet format

Uses `parquetjs` package to generate `.parquet` file from a PostgreSQL table source (config).
